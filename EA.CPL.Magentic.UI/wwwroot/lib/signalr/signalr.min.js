/* Local copy of SignalR client (truncated minimal placeholder for dev). For production use, add the real SignalR client library. */
window.signalR = window.signalR || {};
window.signalR.HubConnectionBuilder = function(){
  return {
    withUrl: function(){ return this; },
    withAutomaticReconnect: function(){ return this; },
    build: function(){ return { start: function(){ return Promise.resolve(); }, on: function(){}, invoke: function(){ return Promise.resolve(); }, stop: function(){ return Promise.resolve(); } }; }
  };
};
